Здравствуйте{% if name %}, {{ name }}{% endif %}!<br/>
Спасибо за ваш заказ в <a href="http://{{ site }}">нашем магазине</a>.<br/>
Ваш заказ номер <b>{{ id }}</b>:
<table style="padding: 10px;" cellspacing="0" cellpadding="0" border="0">
    <tr>
        <td style="padding: 8px; border: 1px solid grey;">Название</td>
        <td style="padding: 8px; border: 1px solid grey;">Бренд</td>
        <td style="padding: 8px; border: 1px solid grey;">шт.</td>
        <td style="padding: 8px; border: 1px solid grey;">Цена за шт.</td>
    </tr>
    {% for g in goods %}
    <tr>
        <td style="padding: 8px; border: 1px solid grey;">{{ g.name }}</td>
        <td style="padding: 8px; border: 1px solid grey;">{{ g.brand.name }}</td>
        <td style="padding: 8px; border: 1px solid grey;">{{ qty }}</td>
        <td style="padding: 8px; border: 1px solid grey;">{{ g.price }}</td>
    </tr>
    {% endfor %}
</table>
<br/>
<b>Итого</b>: {{ count }} шт. на <b>{{ total }}</b> р.<br/><br/>
{% if user_comment %}Ваш комментарий:<br/>
<div style="padding: 8px; border: 1px solid grey;">{{ user_comment }}</div>
{% endif %}

В ближайшее время мы с Вами свяжемся и обсудим все детали доставки!
