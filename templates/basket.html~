
{% extends "base.html" %}

{% load thumbnail %}

{% block title %}Моя корзина - Тема: Outdoor Style{% endblock %}

{% block content %}
  <div class="content_text">
    <br>{% if gs %}
          <table id="bas_t"><caption>Ваш заказ:</caption>
            <tr><td></td>
                <td><b>Артикул</b></td>
                <td><b>Название</b></td>
                <td><b>Категория</b></td>
                <td><b>Бренд</b></td>
                <td><b>Цена</b></td>
                <td><b>Кол-во</b></td>
                <td><b>+1 / -1</b></td>
             </tr>
       {% for g in gs %}
            <tr><td>{% thumbnail g.image_set.all.0.photo "150x150" as thumb %}
                     <a href="/good/{{ g.id }}"><img class="photo" onclick="" src="/media/{{ thumb.url }}" width="{{ thumb.width }}" height="{{ thumb.height }}" alt="{{ good.name }}" /></a>   
	       {% endthumbnail %}</td>
                <td> {{ g.art }} </td>
                <td><b> <a href="/good/{{ g.id }}">{{ g.name }}</a> </b></td>
                <td> {{ g.category }} </td>
                <td> {{ g.brand }} </td>
                <td><b> {{ g.price }} р.</b></td>
                <td><b> {{ g.qty }} </b></td>
                <td><a href="/buy/{{ g.id }}"><img src="/static/img/plus.jpg"></a><br><a href="/delete/{{ g.id }}"><img src="/static/img/minus.jpg"></a></td>
             </tr>
       {% endfor %}
       <tr><td colspan=8>Ваш заказ на общую сумму <b>{{ s }} р.</b> Об условиях доставки Вы можете прочитать <b><a href="/delivery/">здесь</a></b></td>
       </table>
    <br><b><ins>Оформление заказа:</ins></b>
    <form action="/order/" method="POST">
    <label for="name"><b><span class="z">*</span>Имя:</b><input name="name" value="{{ name }}" type="text"></label>
    <label for="name_1"><b>Фамилия:</b><input name="name_1" value="{{ name_1 }}" type="text"></label>
    <label for="name_2"><b>Отчество:</b><input name="name_2" value="{{ name_2 }}" type="text"></label>
    <label for="phone"><b><span class="z">*</span>Телефон:</b><input name="phone" value="{{ phone }}" type="text"></label>
    <label for="email"><b><span class="z">*</span>Email:</b><input name="email" value="{{ email }}" type="text"></label>
    <label for="adress"><b><span class="z">*</span>Адрес:</b><input name="adress" value="{{ adress }}" type="text"></label>
    <label for="comments"><b>Комментарий:</b>
        <textarea name="comments" value="{{ comments }}" rows="4" cols="20"></textarea>
    </label>
    <input value="Отправить заказ!" type="submit" name="order"/>
    </form><span class="z">{{ w }}</span>
    {% else %}<table id="bas_t"><caption> Ваша корзина пока что пуста </caption></table><span class="z">{{ w }}</span>
    {% endif %}

  </div>
{% endblock %}